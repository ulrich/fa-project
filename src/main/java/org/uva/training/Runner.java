package org.uva.training;


public interface Runner {

   /*
   public void run(Collection<Item> items) {
      for (Item item : items) {
         TVATax tvaTax = new TVATax();
         tvaTax.compute(item);
         ImportTax importTax = new ImportTax();
         importTax.compute(item);
         item.getTaxes().add(tvaTax);
         item.getTaxes().add(importTax);
      }
   }*/
}
